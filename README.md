# Market Basket Product Icons

Static product icon PNGs for Algo1 Market Basket deal data.

Images are stored with normalized, URL-safe filenames under `images/`. The original product-name mapping is recorded in `manifest.json`.

Count: 171 images.
